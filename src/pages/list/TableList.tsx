import { PageContainer } from '@epeejs/pro-layout';
import React from 'react';

type TableListProps = Record<string, any>;

const TableList: React.FC<TableListProps> = () => {
  return <PageContainer />;
};

export default TableList;
