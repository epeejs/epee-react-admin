import { PageContainer } from '@epeejs/pro-layout';
import React from 'react';

type BasicFormProps = Record<string, any>;

const BasicForm: React.FC<BasicFormProps> = () => {
  return <PageContainer />;
};

export default BasicForm;
